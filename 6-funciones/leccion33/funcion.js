//Aqui veremos que es y como funciona las funciones en JavaScript

/**
 * La base de las funciones es que nos permiten un codigo o 
 * no repetir una parte del codigo.
 * Una funcion tiene que realizar solo una accion para que 
 * sea sencilla de entender.
 * Las funciones tienen parametros de entrada que nos permite 
 * comunicarnos con los elementos de dentro de la funcion
 * 
 * @param {number} a 
 * @param {number} b 
 */

function miFuncion(a,b) {
    
    console.log("Suma :" + ( a + b ));
    
}

miFuncion(5,10);

miFuncion(1,2);
