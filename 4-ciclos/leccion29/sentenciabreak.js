//Aqui veremos como funciona la sentencia break en bucles como el for en JavaScript.

for (let index = 0; index < 10; index++) {
    
    if (index == 2) {

        console.log(index);

        //Nos permite terminar el bucle cuando queramos sin necesidad de terminar el bucle

        break;
    }
    
}